#include <iostream>
#include <vector>
#include <string>

using std::cerr, std::cout, std::endl;

vector check_variables(const vector &toCheck){
    std::vector<std::string> savedData;
    for(int i = 0; i < len(toCheck); i++){
        if(std::string(toCheck[i]) == std::string(int))
    }
}


int main(int argc, char **argv){
    if(argc != 2){
        cerr << "not enough parameters given" << endl;
    }
}